<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TheoryAssistant"
        tools:targetApi="31">
        <activity
            android:name=".activity.Dashboard"
            android:exported="true"
            android:label="@string/title_activity_dashboard"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>

        <activity
            android:name=".activity.AdminSupport"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar" />

        <activity android:name=".activity.RegistrationAdmin" />

        <activity android:name=".activity.Login" />

        <activity
            android:name=".activity.HintList"/>
<!--            android:label="Hints List" />-->
        <!--            android:parentActivityName=".navigatore_activity.AddHint" />-->
        <activity android:name=".ForgetPassword" />
        <activity android:name=".activity.AddHint" />
        <activity android:name=".activity.MyAccount" />
        <activity android:name=".activity.HintClicked" />
    </application>

</manifest>